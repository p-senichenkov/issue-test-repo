on:
  push:

jobs:
  test-issue:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Create issue
        run: |
          echo ${{ secrets.ISSUE_TEST_TOKEN }} | gh auth login --with-token

          gh issue create --title 'Test issue' --body 'test test test'
